define(function (require) {

    var Util = {
        isBiggerThan: function(a, b) {
            if (a > b) {
                return true;
            } else {
                return false;
            }
        }
    };

    return Util;
});
